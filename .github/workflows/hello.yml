name: My First Action

on: [push]  # Triggers on any code push

jobs:
  test:
    runs-on: ubuntu-latest  # Free GitHub computer (runner)
    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Grabs your repo files
        
      - name: 22f2000618@ds.study.iitm.ac.in  # Your required step name!
        run: echo "Hello from my GitHub Action!"
